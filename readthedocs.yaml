version: 2

formats: all

python:
  # Install our python package before building the docs
  install:
    - method: pip
      path: .
      
sphinx:
  configuration: docs/conf.py
  fail_on_warning: true
    
