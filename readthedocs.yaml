version: 2

formats: all

sphinx:
  configuration: docs/conf.py
  
python:
  # Install our python package before building the docs
  install:
    - method: pip
      path: .
      
build:
  os: ubuntu-20.04
  tools:
    python: "3.8"
